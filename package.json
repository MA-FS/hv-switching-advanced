{
  "name": "hv-switching-advanced",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "bootstrap-icons": "^1.11.3"
  },
  "scripts": {
    "predeploy": "cd switching-program && npm run build",
    "deploy": "gh-pages -d switching-program/build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
